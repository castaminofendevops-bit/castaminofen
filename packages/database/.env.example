# Prisma reads env from the monorepo root via dotenv-cli in package.json scripts.
# Copy ../../.env from repo root — or set DATABASE_URL here for standalone runs:
DATABASE_URL=postgresql://castaminofen:castaminofen@127.0.0.1:5432/castaminofen
